dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3
dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  sensors_plus: ^5.0.0
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3
dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  image: ^4.5.3
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3
flutter_blue_plus: ^1.32.0
dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+1
  image: ^4.5.3
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3
  flutter_blue_plus: ^1.32.0   # BLE for mesh
dependencies:
  encrypt: ^5.0.3